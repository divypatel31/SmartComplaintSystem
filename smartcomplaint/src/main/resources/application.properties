# Connect to your existing MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/smart_complaint_db
spring.datasource.username=root
spring.datasource.password=Divypateln@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Keep your existing tables safe, but update them if needed
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Run the server on port 8080
server.port=8080